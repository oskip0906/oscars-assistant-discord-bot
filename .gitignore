node_modules
.env
data/

# generated by run.sh — the supervisor must never be rewritten by a git pull
# while it is running (see the template in run.sh)
start.sh

.DS_Store
npm-debug.log*

# local searxng secret (generated per-machine)
searxng/config/settings.yml

# YouTube auth cookies — never commit
cookies.txt
*cookies*.txt

# Written at runtime by /set_rule and read back into the system prompt. It is
# per-deployment state, not source: committing it would ship one machine's rules
# to every checkout and hand the development sandbox a file it must never edit.
context/instructions.md
